static_sites:
  - name: kidzone-frontend
    type: static
    buildCommand: cd frontend && npm install && npm run build
    publishDir: frontend/dist
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
